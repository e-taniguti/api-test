meta {
  name: Delete a book
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/books/:id
  body: none
  auth: none
}

params:path {
  id: {{bookId}}
}

headers {
  api-key: {{apiKey}}
}

script:pre-request {
  bru.setVar('bookId' , bru.getEnvVar('bookId'))
}
